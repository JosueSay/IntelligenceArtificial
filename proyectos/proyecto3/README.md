# Testing

Para probar el código se deberá guardar `test_ai.py` en cliente y reeemplazar las funciones `def ai_move(board, player):` de `othello_ai.py` y agregar una funcion para el testing llamada `move` de `utils.py` al final de `othello_ai.py`.
